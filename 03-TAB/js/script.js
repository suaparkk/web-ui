$(function () {
  // 대상을 변수에 저장
  const tabMenu = $('.tab-menu > li');
  const tabContent = $('.tab-con-item');

  console.log(tabMenu, tabContent);

  // 초기 세팅

  // 첫번재 탭메뉴가 활성화되게끔

  // 탭메뉴를 클릭했을 때

  // 클릭한 놈의 인덱스를 구해서 변수에 담고

  // 탭 콘텐츠 중엥서 인덱스에 해당하는 내용을 보여지게
});
